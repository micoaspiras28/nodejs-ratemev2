<% layout('landing-page') %>

<div class="d-flex" id="wrapper">
  <% include ../partials/landing-page/sidebar %>
    <div id="page-content-wrapper">
      <% include ../partials/landing-page/navbar %>

        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="ul-links">
                <ul>
                    <li><a href="/home">Home</a></li> |
                        <li><a href="">Company</a></li>|
                        <li><a href="">Register Company</a></li>
                </ul>
            </div>
        </nav>
        <div class="container-fluid">
            <div class="body-div">
                <h4 class="title">Companies</h4>
            <div class="row">
                <% for(var i = 0; i < data.length; i++) {%>
                <div class="column">
                    <div class="card">
                        <a href="/company-profile/<%= data[i]._id %>">
                        <img class="card-img-top" width="270px" height="240px;"  src="/uploads/<%= data[i].image %>" alt="Card image cap"> 
                        <div class="card-body">
                            <h5 class="card-title"><%= data[i].name %></h5></a><hr>
                                <span><i class="fa fa-star"></i></span><br>
                                <i class="fa fa-building-o"></i> <%= data[i].sector %><br>
                                <i class="fa fa-map-marker"></i> <%= data[i].city %> , <%= data[i].country %>
                        </div> 
                        <div class="card-footer">
                            <small class="text-muted">Last updated 3 mins ago</small>
                        </div>
                    </div>
                </div>
                <% } %> 
            </div>
        </div>   
    </div>
</div>
   
   
